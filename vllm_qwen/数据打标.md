数据集：E:\Image_Decomposition\code\data\RevealLayer-100K\Benchmark\RevealLayerBenchMark-200
推理代码：E:\Image_Decomposition\code\vllm_qwen\infer.py
数据json：E:\Image_Decomposition\code\data\RevealLayer-100K\Benchmark\RevealLayerBenchMark-200\metaData.json

现状：
1.这个数据集为多层图像数据集，包括干净的rgb背景，若干rgba前景以及组合得到的完整图像。图像按照顺序从上到下组合为完整图像：layer_0.png，layer_1.png，...，background.png。
2.数据集中的数据格式为：若干子文件夹，每个子文件夹的名字是唯一的，为图片的id；数据json为数据的统计，里面的每一条数据都代表一个子文件夹，其中有完整图像，背景图像以及前景图像的相对路径，以及每一个前景图像的bbox，格式应该是(x1, y1, x2, y2)
3.推理代码是调用本地qwen3.6来进行数据集制作的代码，输入是图像和文本，进行图像的理解。

要求：
1.需要你基于推理代码重新写一个推理脚本，将数据集的数据进行打标。目前的数据只有bbox，需要你将图像输入模型，得到图像的prompt。
2.图像的prompt包括全局图像的prompt以及每一个前景的prompt和背景的prompt。全局图像的prompt需要包括每一个前景以及背景。
3.需要你设计一个合理的全局prompt，指引qwen对图像进行打标。得到的prompt需要写入数据json中，需要对json重新设计。按照layer_0，layer_1,，...，背景，全局图像的顺序将一个图像的数据条目拆分为若干条，每个条目需要有图像的相对路径，图像的prompt，图像的bbox；背景的bbox设置为null；全局图像的bbox设置为\[layer_0的bbox, layer_1的bbox, ... \]，将所有的bbox按照顺序放在里面。

预期：
1.设计一个推理的脚本，其中有合理的全局prompt指引，直接读取数据json和数据路径。按照数据json的条目提取图像进行打标。每一次推理将一张图像的所有内容输入模型，包括背景，若干前景以及全局图像。输出一个新的json，按照要求中的格式将识别的prompt保存下来。
2.输出一个readme.md，介绍数据保存的格式，推理代码的逻辑与运行方法。